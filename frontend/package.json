{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cf:deploy": "npm run build && wrangler pages deploy dist --project-name fixturecast",
    "cf:dev": "npm run build && wrangler pages dev dist --port 8788"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "svelte": "^4.2.20",
    "tailwindcss": "^3.4.18",
    "terser": "^5.44.1",
    "vite": "^5.4.21",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "lucide-svelte": "^0.554.0",
    "svelte-i18n": "^4.0.1",
    "svelte-routing": "^2.13.0"
  }
}
